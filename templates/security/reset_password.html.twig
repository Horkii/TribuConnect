{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe • TribuConnect{% endblock %}

{% block body %}
  <h1>Réinitialiser le mot de passe</h1>
  <p>Vous réinitialisez le mot de passe du compte associé à l'adresse <strong>{{ email }}</strong>.</p>
  <form method="post" action="{{ path('app_reset_password', {token: token}) }}" style="max-width:420px;">
    <input type="hidden" name="_token" value="{{ csrf_token('reset_password_' ~ token) }}">
    <label>Nouveau mot de passe
      <input type="password" name="password" required autocomplete="new-password">
    </label>
    <label>Confirmation
      <input type="password" name="password_confirm" required autocomplete="new-password">
    </label>
    <button class="btn" type="submit">Changer le mot de passe</button>
  </form>
{% endblock %}

